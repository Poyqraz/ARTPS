\FloatBarrier
\section{Discussion}
\label{sec:discussion}
ARTPS is an operational target-prioritization architecture in which anomaly cues and relative
depth feed fusion, localization, suppression, diversity, and buffering, with explicit
no-selection when evidence is insufficient.
The design targets structured prioritization within a bounded rover operating envelope.
Operational target selection combines anomaly response with validity, spatial context,
suppression, and ranking; distractors (shadows, hardware, borders) inflate cue responses and
are handled by the operational stack.
Relative depth supplies near/far structure for apparent-size policy as an image-relative
ordering cue~\citep{ranftl2021dpt}.
Diversity management reduces redundant near-duplicates under limited downlink slots;
the Priority Buffer keeps high-anomaly candidates that the similarity penalty would otherwise
drop irreversibly.
The reported image-level validation terminates at Layer~B; Layer~C supplies downstream
operational ranking.
Fig.~\ref{fig:qualitative} visualizes Layer~B products: the post-suppression combined map,
relative depth, and surviving candidates.
Fig.~\ref{fig:cue-decomp} decomposes the existing Layer~B fusion into its principal visual
cues and the pre-suppression fused map.
The single-scene example serves qualitative mechanism inspection; benchmark evidence is
provided by the quantitative evaluation.
Candidate actionability emerges from the full validity and ranking sequence beyond local
cue magnitude alone.
Read left to right, the panels separate appearance, the post-suppression combined map,
image-relative depth, and the candidates that remain after operational masks.
The overlay represents a Layer~B localization hypothesis supported by candidate-region
evidence; where available, a translucent support footprint marks that image-region evidence.
Curiosity ranking and science-target confirmation occur downstream.

The entropy-weighted fusion rule remains the specified adaptive fusion mode.
The human-reviewed validation uses the fixed-coefficient image-level scoring configuration.
Table~\ref{tab:eval-tracks} summarizes the primary performance evaluation and the
human-reviewed validation.
The initial binary labels were produced by a classical-image heuristic with forced $180/180$
class balance.
A model-blind repeat-author review produced $340$ positive / $20$ negative labels.
The reviewed labels more directly reflect the adopted human annotation definition than the
initial heuristic label layer.
This observed prevalence is specific to the sampled Curiosity Mastcam domain and the
adopted science-interest annotation definition; generalization to planetary imagery as a
whole remains outside the present evidence scope.
The human-reviewed corpus contains $340$ positive and $20$ negative images.
On the validation partition, the ARTPS image-level score achieved AUROC~$0.772$.
Because the validation partition contains eight negative images and the predeclared
operating point yielded all-positive predictions, the result is interpreted primarily as
ranking evidence on that partition.
The small number of negative validation examples also limits the precision of the reported
AUROC estimate.
The all-positive operating point renders threshold-dependent F1 uninformative for this
validation; AUROC and AP therefore characterize ranking performance.
A future balanced challenge set or harder negative set may be useful for operating-point
evaluation.

The $28.1$\,FPS measurement characterizes the lightweight preprocessing--fusion--localization
core on the development workstation.
Full-pipeline edge-hardware timing and flight qualification constitute separate hardware and
mission V\&V stages.
