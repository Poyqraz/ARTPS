\section{ARTPS Methodology}
This section describes the ARTPS pipeline as implemented for the present study.
Quantitative performance claims appear in Section~\ref{sec:results}.

\begin{figure*}[t]
\centering
\input{figures/artps_pipeline}
\caption{ARTPS architecture.
Layer~A generates optional enhancement, reconstruction, relative-depth, and texture cues.
The classifier contributes a known-value scalar directly to candidate scoring.
Layer~B is the image-level scoring path: cue normalization, fusion,
proximity modulation, map-level suppression, localization, candidate scoring with
validity gates, and aggregation of remaining valid candidates.
Entropy-weighted fusion is the specified adaptive fusion mode (dashed).
The human-reviewed validation uses the fixed-coefficient image-level scoring configuration.
Layer~B terminates at the image-level score, while Layer~C consumes valid candidates for
curiosity-, diversity-, and buffer-based operational ranking.}
\label{fig:pipeline}
\end{figure*}

\subsection{System overview}
ARTPS ingests a single RGB field image and produces a ranked list of candidate targets, or an
explicit no-selection outcome when evidence is insufficient.
Primary modules are: (i)~multi-cue anomaly responses, (ii)~monocular relative depth,
(iii)~multi-cue fusion (entropy-weighted specification as the adaptive fusion mode),
(iv)~candidate localization, (v)~operational false-positive
suppression, (vi)~apparent size / image-relative size--distance policy,
(vii)~feature-space diversity, (viii)~Priority Buffer, and (ix)~runtime fail-safe behaviour
with evaluation-integrity checks.
The image-level ARTPS score is defined as the maximum score among candidates that remain
valid after operational filtering.
The human-reviewed validation uses this aggregation.
Image-level scoring terminates at Layer~B.
Curiosity, feature-space diversity, and the Priority Buffer operate subsequently in Layer~C.
Optional Mars-oriented enhancement is confined to preprocessing upstream of cue extraction.
Reported experiments use the native-resolution preprocessing configuration, with the
super-resolution module inactive.

\subsection{Multi-cue visual anomaly detection}
ARTPS forms visual anomaly responses from embedding- or reconstruction-oriented cues aligned
with industrial anomaly practice~\citep{defard2020padim,roth2022patchcore}.
A reconstruction residual captures deviations from reconstructions of typical terrain
context.
Complementary texture and local-contrast cues capture high-frequency structure that a
reconstruction residual may under-emphasize, including sharp rock edges and mottled surfaces.
Fixed-coefficient multi-cue fusion uses a relative-depth \emph{edge} cue (image-relative depth
discontinuity); protrusion remains a candidate geomorph descriptor outside the fused score.
The depth-edge cue highlights regions that stand out from the surrounding near/far layout
relative to appearance cues alone.
Cue maps therefore highlight unusual appearance or structure relative to a reference
distribution of ``normal'' terrain context.
Each cue serves as an input to fusion and localization.
Mineralogical or lithological interpretation requires dedicated sensing and labels beyond
these anomaly cues.
Fig.~\ref{fig:qualitative}(b) shows the post-suppression combined anomaly map on a non-test
example.

\subsection{Monocular relative depth}
A dense monocular estimator provides an image-relative depth field for near/far
organization~\citep{ranftl2021dpt,ranftl2022robust}.
ARTPS maps monocular depth to a within-image relative field used for near/far ordering,
depth-edge extraction, and apparent-size policy.
The representation is non-metric; absolute range and physical dimensions require calibrated
geometry.
Depth supports apparent size--distance policy and spatial structuring of candidates, and it
feeds the depth-edge cue used in fixed-coefficient multi-cue fusion.

\begin{figure*}[!t]
\centering
\includegraphics[width=0.80\textwidth]{figures/fig_qualitative_artps.png}
\caption{Qualitative ARTPS processing example from a non-test Mars scene
selected before inference using predefined scene-composition criteria.
Panels show the RGB input, the post-suppression combined anomaly map, monocular image-relative
depth, and the candidate-support overlay produced by the ARTPS image-level scoring path.
Corner brackets denote the approximate candidate ROI.
Where proposal support geometry is available, the translucent footprint visualizes candidate
proposal-support geometry at image-region level.
The depth visualization is relative and non-metric.
The example provides qualitative process visualization; quantitative evaluation is reported
separately in Tables~\ref{tab:accepted-abstract}--\ref{tab:indep-v11}.}
\label{fig:qualitative}
\end{figure*}

Fig.~\ref{fig:qualitative}(c) shows this image-relative near/far field.

\subsection{Entropy-weighted dynamic fusion}
Cue reliability can vary across illumination and terrain.
The ARTPS specification defines an entropy-weighted fusion rule: for normalized cue maps
\(A_i'\), Shannon entropy \(H_i=-\sum_k p_{ik}\log(p_{ik}+\varepsilon)\) yields
\(\tilde w_i=1/(H_i+\varepsilon)\) and \(w_i=\tilde w_i/\sum_j\tilde w_j\), so
\(A_{\mathrm{comb}}=\sum_i w_i A_i'\).
Low-entropy (peaked) maps receive higher weight.
Those normalized entropy weights sum to one.
For the human-reviewed validation, Layer~B uses the \emph{fixed-coefficient} linear mix
defined below; entropy-weighted fusion remains the adaptive specification.
Let \(R\), \(E_D\), \(T\), \(L_D\), and \(F_D\) denote inference-normalized reconstruction
residual, relative-depth Sobel edge, texture / local-contrast, depth-Laplacian, and
fine-detail maps (percentile~$2$--$98$ per cue), and let \(P\) be the normalized proximity
map (near high).
The fixed-coefficient mix is
\[
A_{\mathrm{raw}}
=
0.50\,R + 0.30\,E_D + 0.20\,T + 0.08\,L_D + 0.12\,F_D,
\]
\[
P_{\mathrm{mix}}=0.55\,P+0.45\,(1-P),
\]
\[
A_{\mathrm{pre}}
=
\mathrm{clip}\!\bigl(A_{\mathrm{raw}}\,[0.5+0.5\,P_{\mathrm{mix}}],\,0,1\bigr).
\]
The values \(0.50/0.30/0.20/0.08/0.12\) are \emph{fixed fusion coefficients}: they are
implementation weights followed by proximity modulation and clipping; unit-sum normalization
is therefore unnecessary.
They are distinct from the entropy-weighted architectural specification.
The evaluated configuration uses fixed, manually specified coefficients.
The proximity multiplier lies in a narrow band near \(0.75\), so far-field responses receive
reduced but nonzero weight.
That fixed-coefficient mix is the Layer~B path used in the human-reviewed validation
(Fig.~\ref{fig:pipeline}).
Layer~B produces the image-level score; Layer~C operates on the surviving candidate set for
downstream ranking.
\Needspace{3\baselineskip}
Accordingly, the human-reviewed metrics characterize Layer~B image-level scoring, while
Layer~C processes valid candidates for operational ranking.

\begin{figure*}[!t]
\centering
\includegraphics[width=0.80\textwidth]{figures/fig_cue_decomposition_artps.png}
\caption{Cue decomposition of the fixed-coefficient multi-cue score on the same non-test image as
Fig.~\ref{fig:qualitative}.
Panels show reconstruction residual, relative-depth edge, texture / local contrast, and the
pre-suppression fused map after proximity mixing.
The four displayed panels emphasize the dominant reconstruction, relative-depth-edge, texture,
and fused responses; depth-Laplacian and fine-detail terms also contribute to the fusion.
Independent min-max display scaling assigns each panel its own visualization range;
cross-panel color intensity therefore serves qualitative inspection.
The depth-derived cue is image-relative and non-metric.
Quantitative performance is reported separately in
Tables~\ref{tab:accepted-abstract}--\ref{tab:indep-v11}.}
\label{fig:cue-decomp}
\end{figure*}

Fig.~\ref{fig:cue-decomp} shows how that fixed-coefficient mix is assembled on the same non-test image
as Fig.~\ref{fig:qualitative}.
Panel~(a) is the per-channel reconstruction residual after inference percentile normalization.
Panel~(b) is the relative-depth edge (Sobel magnitude of the monocular depth field): an
image-relative discontinuity cue used for fusion.
Panel~(c) is the texture / local-contrast term (shadow-aware darkening mixed with image
gradient magnitude).
Panel~(d) is the pre-suppression fused map \(A_{\mathrm{pre}}\).
\Needspace{4\baselineskip}
The classifier / known-value term is enabled in the implemented Layer~B configuration for
candidate scoring and curiosity; it enters candidate scoring and remains outside the fused map.
Display scaling in the figure is independent of inference percentile normalization and serves
qualitative inspection of the existing Layer~B mix.

\Needspace{8\baselineskip}
\subsection{Candidate localization}
Fused anomaly responses are converted into discrete candidate regions via localization
(thresholding / connected components / peak extraction; implementation detail is in the
accompanying software).
Each candidate carries a score, spatial extent, and optional feature descriptor for diversity.
Validity gates restrict image-level aggregation to surviving candidates.
Image-level aggregation uses the maximum candidate score over \(R_{\mathrm{valid}}\);
curiosity-weighted ranking occurs later in Layer~C.
Fig.~\ref{fig:qualitative}(d) visualizes the Layer~B surviving-candidate set prior to Layer~C
ranking.

\subsection{Operational false-positive suppression}
Rover imagery frequently contains distractors: rover body / hardware, strong boundary shadows,
frame edges, and telemetry overlays.
These regions can produce high reconstruction or texture responses outside the science-interest
annotation guide.
ARTPS applies operational masks and suppressions so that high responses in these regions are
down-weighted or invalidated before ranking.

Boundary-connected shadow suppression targets dark, low-structure regions connected to the
image boundary, including cast-shadow wedges and similar artefacts, while leaving unstructured
dark terrain subject to the remaining validity gates.
Rover-body and nearby hardware are excluded as self-view clutter.
Frame-edge and overlay / telemetry regions are invalidated where those signals are available.
The object-in-shadow gate preserves structured candidates within shadowed regions while
suppressing flat boundary-shadow artefacts.
These masks act before image-level score aggregation.
The masks enforce candidate-validity constraints; mission-level science assessment occurs
downstream of candidate-validity filtering.

Image-level aggregation includes candidates that satisfy all validity gates;
Fig.~\ref{fig:qualitative}(d) shows that candidate-support overlay on one non-test
example.
A surviving candidate remains a localization hypothesis and proceeds to downstream ranking
before downlink selection.
Suppression is policy-driven and inspectable.
Downstream ranking processes surviving candidates, while mission-specific V\&V characterizes
residual distractor risk.

\subsection{Candidate and image-level score}
After localization and validity gates, each remaining candidate \(r\) receives a score
from pooled map responses and a classifier-derived known-value scalar.
Let \(F_r\) be the pooled combined-map response, \(D_r\) the pooled relative-depth value,
\(Q_r\) the proposal confidence, and \(K\) the image-global known-value scalar
(\(\arg\max\) class index divided by four when the classifier is enabled).
$K$ is an image-global known-value scalar used exclusively as a candidate-scoring feature;
its semantics exclude per-box and lithological interpretation.
The ARTPS scoring function supports optional PaDiM and PatchCore responses.
The human-reviewed Layer~B configuration fixes the PaDiM and PatchCore score terms to zero;
PaDiM/PatchCore performance is reported through the separate baseline evaluation.
The candidate equations are
\[
V_r=\mathrm{clip}(0.55\,K+0.25\,D_r+0.20\,F_r,0,1),
\]
\[
\begin{aligned}
A_r=\mathrm{clip}\bigl(&0.50\,F_r+0.20\,P^{\mathrm{PaDiM}}_r+0.15\,P^{\mathrm{PC}}_r\\
&+0.10\,Q_r+0.05\,(1-D_r),0,1\bigr),
\end{aligned}
\]
\[
S_r=\mathrm{clip}\bigl(A_r\,(0.70+0.30\,V_r),0,1\bigr).
\]
The image score is
\[
S_{\mathrm{image}}=\max_{r\in R_{\mathrm{valid}}} S_r,
\]
or \(S_{\mathrm{image}}=0\) when no valid candidate remains.
A slim recall-support pooling step may raise \(F_r\) for some proposal sources before these
equations.
Recall-support pooling modifies \(F_r\) upstream while the equations above remain the sole
candidate-score formulation.

\subsection{Apparent size / image-relative size--distance policy}
Given relative depth and image extent, ARTPS applies an image-relative size--distance policy
to prefer candidates whose apparent size is consistent with the intended operational envelope
(e.g., avoiding both tiny noise peaks and unrealistically large blobs under relative depth
ordering).
The policy operates in normalized image/depth coordinates; metric physical size requires
calibrated geometry.

\subsection{Operational ranking sequence}
Candidates that remain valid after perception and masks enter an operational ranking
stack for onboard downlink.
The sequence is: a surviving candidate receives a curiosity score; a soft similarity penalty
then reduces its priority among already selected items; the Priority Buffer retains a
high-anomaly candidate that lost that diversity contest for later reconsideration.
Image-level scoring ends before the Layer~C operational-ranking sequence.

\subsection{Curiosity score}
For operational ranking, ARTPS computes a curiosity score as a non-negative weighted
combination of available region cues (known-value / classifier term, reconstruction or
anomaly response, optional combined-map, depth variance, and roughness),
\[
C(r)=\sum_i w_i S_i(r),\qquad w_i\ge 0.
\]
Default deployed weights emphasize known-value and anomaly terms.
\(C(r)\) is reserved for Layer~C operational ranking.

\subsection{Feature-space diversity}
To avoid redundant near-duplicates in the priority list, ARTPS applies a soft similarity
penalty in feature space to the fused candidate score \(S_r\).
If \(s(r)\) is the maximum cosine similarity to already selected items and
\(\lambda\in[0,1]\),
\[
S'_r=S_r\,(1-\lambda s(r)).
\]
Diversity improves coverage of distinct visual modes under limited downlink slots.
Diversity acts after Layer~B image scoring.

\subsection{Priority Buffer}
Aggressive diversity down-weighting can deprioritize rare but potentially valuable candidates.
The Priority Buffer retains high-anomaly candidates that lose the diversity contest:
\[
r\in\mathcal{B}\iff S_r>\tau_{\mathrm{high}}\ \mathrm{and}\ \bigl(S_r-S'_r\bigr)>\tau_\Delta.
\]
Priority Buffer eligibility is restricted to candidates that pass Layer~B validity gates.
The buffer provides second-chance ranking within Layer~C, while the image-level score remains
fixed at the Layer~B output.

\subsection{Runtime and safety-aware fail-closed behaviour}
ARTPS uses an edge-oriented processing architecture with bounded compute.
Runtime processing returns an explicit no-selection outcome when evidence is insufficient.
Incomplete required inputs trigger deterministic abort/no-selection behaviour.
Safety bounds are stated in Section~\ref{sec:limitations}.
