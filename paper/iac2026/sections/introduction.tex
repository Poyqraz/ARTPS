\section{Introduction}
Planetary rover missions operate under tight energy limits, constrained communications bandwidth,
and delayed ground feedback~\citep{estlin2012,francis2017aegis}.
Acquired images vary in scientific value for follow-up imaging or downlink, so
onboard systems must prioritize candidate targets according to operational actionability.

Classical visual anomaly detection provides useful cues for unusual appearance; operational
target selection additionally depends on localization, false-positive suppression, diversity,
and ranking under rover constraints.
Field imagery is rich in distractors: cast shadows, rover hardware, image borders, telemetry
overlays, and textured terrain can inflate anomaly responses and produce false positives.
Monocular depth estimators provide image-relative near/far structure for ordering and policy
decisions~\citep{ranftl2021dpt}.
Onboard autonomy further requires attention to computational cost, diversity among selected
targets, and safety-aware fallbacks when evidence is insufficient.

This paper presents ARTPS (Autonomous Rover Target Prioritization System), an edge-oriented
framework that couples multi-cue visual anomaly responses with monocular relative depth,
entropy-weighted dynamic fusion, candidate localization, operational false-positive
suppression, apparent size / image-relative size--distance policy, feature-space diversity,
and a Priority Buffer that retains high-anomaly candidates after diversity down-weighting
for second-chance evaluation.

\newpage
\vspace*{5pt}
The present study evaluates ARTPS as a safety-aware, flight-readiness-oriented software
architecture within a defined operational envelope.
Flight qualification and mission certification require independent mission-specific V\&V.

The contributions of this work are as follows:
\begingroup
\setlength{\itemsep}{2pt plus 0pt minus 1pt}
\setlength{\parsep}{0pt}
\setlength{\topsep}{2pt plus 0pt minus 1pt}
\setlength{\partopsep}{0pt}
\enlargethispage{2\baselineskip}
\begin{enumerate}
  \item A hybrid onboard target-prioritization pipeline that combines visual anomaly cues with
  monocular relative-depth structure for field imagery.
  \item An entropy-weighted dynamic fusion specification that adapts cue weights from cue
  reliability.
  The human-reviewed validation uses the fixed-coefficient image-level scoring configuration.
  \item Localization with apparent size--distance policy and diversity-aware ranking to
  structure candidates for operational review.
  \item A Priority Buffer that retains high-anomaly candidates down-weighted by diversity
  policy for second-chance evaluation.
  \item Operational suppression mechanisms addressing rover-body, boundary-shadow, and related
  imagery artefacts that commonly drive false positives.
  \item A two-component evaluation comprising a primary detection and runtime study and an
  additional human-reviewed validation of image-level ranking.
\end{enumerate}
\endgroup
% Avoid unverified ``first'' / ``state-of-the-art'' claims; contributions are architectural and operational.
