\FloatBarrier
\section{Results}
\label{sec:results}

\subsection{ARTPS Performance}
ARTPS achieved an AUROC of $0.894$, an AUPRC of $0.847$, and an F1 score of $0.823$ on NASA
Mars rover imagery.
The PaDiM/PatchCore baseline achieved an AUROC of $0.856$.
In the lightweight configuration, the preprocessing--fusion--localization core achieved
$28.1$\,FPS at $256\times256$ on the development workstation
(Table~\ref{tab:accepted-abstract}).

\begin{table}[t]
\centering
\caption{ARTPS detection and lightweight runtime performance.}
\label{tab:accepted-abstract}
\begin{tabular}{@{}lc@{}}
\toprule
Metric & Value \\
\midrule
AUROC & $0.894$ \\
AUPRC & $0.847$ \\
F1 & $0.823$ \\
Baseline AUROC & $0.856$ \\
FPS @ $256\times256$ (lightweight core) & $28.1$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Human-Reviewed Validation}
On the 54-image validation partition ($46$ positive, $8$ negative), the fixed image-level
ARTPS score yielded AUROC~$0.772$ and AP~$0.956$
(Table~\ref{tab:indep-v11}).
These results characterize the Layer~B image-level score before Layer~C operational ranking.
AUROC is the principal ranking-oriented metric for this analysis.

\begin{table}[t]
\centering
\caption{Human-reviewed ARTPS validation results.}
\label{tab:indep-v11}
\small
\begin{tabular}{@{}lc@{}}
\toprule
Item & Value \\
\midrule
\multicolumn{2}{@{}l}{\emph{Annotation review (360 images)}} \\
Reviewed / unchanged / relabeled & $360$ / $188$ / $172$ \\
Initial heuristic labels (pos./neg.) & $180$ / $180$ \\
Human-reviewed labels (pos./neg.) & $340$ / $20$ \\
\midrule
\multicolumn{2}{@{}l}{\emph{Validation subset ($46$ pos.\ / $8$ neg.)}} \\
AUROC (principal ranking metric) & $0.772$ \\
AP (val.\ prevalence $46/54 \approx 0.852$) & $0.956$ \\
\bottomrule
\end{tabular}
\end{table}

The predeclared F1-based validation threshold selection collapsed to an
all-positive operating point.
The all-positive operating point makes threshold-dependent classification metrics
non-discriminative; AUROC serves as the principal ranking measure.

\subsection{Qualitative processing example}
Fig.~\ref{fig:qualitative} is a single non-test illustration of RGB input, the
post-suppression combined anomaly map, relative depth, and candidate-support overlay under the
ARTPS image-level scoring path.
Fig.~\ref{fig:cue-decomp} is a diagnostic decomposition of the existing Layer~B fusion into
reconstruction, relative-depth-edge, and texture cues plus the pre-suppression fused map.
\begin{figure*}[!t]
\centering
\includegraphics[width=0.96\textwidth]{figures/fig_close_far_qualitative_artps.png}
\caption{Qualitative examples of the ARTPS pipeline on a close-range and a distant
non-test scene.
Each row shows the RGB input, post-suppression combined map, and candidate-support
overlay.
Corner brackets indicate the approximate candidate ROI; where proposal geometry is
available, the translucent footprint visualizes candidate proposal-support geometry at
image-region level.
The examples provide qualitative process visualization; quantitative evaluation is reported
separately in Tables~\ref{tab:accepted-abstract}--\ref{tab:indep-v11}.
Fig.~\ref{fig:close-far} provides qualitative apparent-scale context across close and distant
scenes; range-conditioned performance remains outside this analysis.}
\label{fig:close-far}
\end{figure*}
In addition to that single-scene example and cue decomposition, Fig.~\ref{fig:close-far}
shows one close-range and one distant-scene illustration from preselected non-test scenes,
to show how the ARTPS pipeline behaves under different apparent object-scale
contexts.
